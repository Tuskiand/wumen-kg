# 不同医家同病辨证图谱点线面比较方案

## 一、研究目标

本研究基于当前中医医案知识图谱，比较不同医家治疗同一种病名时，在辨证认识上的相同点和不同点。

当前规则只允许 5 类实体：

| 实体类型 | 含义 |
|---|---|
| `A医家` | 医家 |
| `B病名` | 病名 |
| `C证型` | 证型 |
| `D病因` | 病因 |
| `E病机` | 病机 |

当前规则只允许 10 类关系：

| 头实体 | 关系 | 尾实体 |
|---|---|---|
| `A医家` | `A医家-B病名` | `B病名` |
| `A医家` | `A医家-C证型` | `C证型` |
| `A医家` | `A医家-D病因` | `D病因` |
| `A医家` | `A医家-E病机` | `E病机` |
| `B病名` | `B病名-C证型` | `C证型` |
| `B病名` | `B病名-D病因` | `D病因` |
| `B病名` | `B病名-E病机` | `E病机` |
| `C证型` | `C证型-D病因` | `D病因` |
| `C证型` | `C证型-E病机` | `E病机` |
| `D病因` | `D病因-E病机` | `E病机` |



## 二、总体路线

采用“点、线、面”三层比较。

| 层级 | 比较对象 | 解决的问题 | 难度 |
|---|---|---|---|
| 点 | 证型、病因、病机节点 | 不同医家关注哪些相同或不同的辨证要素 | 低 |
| 线 | `医家 -> 病因 -> 病机 -> 证型` 辨证链 | 不同医家的辨证思路是否相同 | 中 |
| 面 | 医家围绕同一病名形成的子图 | 不同医家的整体辨证结构是否相似 | 高 |

推荐实施顺序：

| 顺序 | 内容 | 产出 |
|---|---|---|
| 1 | 点比较 | 共同证型、共同病因、共同病机、独有节点 |
| 2 | RWR核心节点排序 | 每位医家的核心辨证节点 |
| 3 | 线比较 | 共同辨证链、独有辨证链、路径完整度 |
| 4 | 面比较 | 基于子图比较得到医家相似矩阵 |
| 5 | NetLSD + FGSD + Graph2Vec 向量实验 | 结构向量对照结果，不作为主结论 |

## 三、节点比较方案

节点比较用于回答：

| 问题 | 示例 |
|---|---|
| 哪些证型是多位医家共同提到的 | 多位医家都关联 `痰热内闭证` |
| 哪些病因是某位医家独有的 | 某医家更强调 `风邪外袭` |
| 哪些病机是共同核心认识 | 多位医家都关联 `痰瘀阻络` |

### 3.1 Node Similarity

Node Similarity 是 Neo4j GDS 的节点相似度算法框架。可以使用不同相似度度量： Jaccard、Overlap 、Cosine

| 度量方式 | 通俗解释 | 适用原因 |
|---|---|---|
| `JACCARD` | 共同邻居数量 / 全部邻居数量 | 最适合作为主指标，直观说明共同辨证要素比例 |
| `OVERLAP` | 共同邻居数量 / 较小邻居集合数量 | 医家资料量不均时更公平 |
| `COSINE` | 把邻居关系看成向量后比较方向 | 如果节点或关系有权重，可做加权比较 |

推荐写法：

> 本研究采用 Neo4j GDS Node Similarity 算法，并分别使用 Jaccard、Overlap、Cosine 三种相似度度量，比较不同医家在证型、病因、病机邻接集合上的相似程度。

### 3.2 RWR 带重启随机游走

RWR 用于找核心节点，不是单纯相似度算法。

| 内容 | 说明 |
|---|---|
| 起点 | 某医家 + 某病名 |
| 游走范围 | `C证型`、`D病因`、`E病机` |
| 输出 | 每位医家的核心证型、核心病因、核心病机 |
| 作用 | 解释某医家治疗某病时最重视什么 |

推荐用途：

| 用途 | 说明 |
|---|---|
| Node Similarity | 比较医家之间像不像 |
| RWR | 找每位医家的重点是什么 |

节点层最终输出：

| 输出 | 说明 |
|---|---|
| 医家相似度矩阵 | 每两位医家的 Jaccard、Overlap、Cosine 分数 |
| 共同节点 | 多位医家共同连接的证型、病因、病机 |
| 独有节点 | 某位医家特有的证型、病因、病机 |
| RWR Top-K | 每位医家的核心辨证节点排名 |

## 四、辨证路径比较方案

辨证路径比较必须符合临床辨证思路：

`A医家 -> D病因 -> E病机 -> C证型`

也就是：

| 顺序 | 含义 |
|---|---|
| `A医家` | 哪位医家 |
| `D病因` | 认为病从哪里来 |
| `E病机` | 病在体内如何发展变化 |
| `C证型` | 最后归纳成什么证型 |

注意：数据库里的关系方向仍严格服从 `relations.md`，不擅自反转、不伪造关系。线层只是分析展示时按临床逻辑重排。

### 4.1 路径完整度

| 类型 | 路径 | 含义 |
|---|---|---|
| 完整链 | `A医家 -> D病因 -> E病机 -> C证型` | 病因、病机、证型都完整 |
| 较完整链 | `A医家 -> D病因 -> E病机` | 有病因和病机，证型缺失 |
| 较完整链 | `A医家 -> E病机 -> C证型` | 有病机和证型，病因缺失 |
| 简化链 | `A医家 -> D病因 -> C证型` | 有病因和证型，病机缺失 |
| 单点链 | `A医家 -> D病因 / E病机 / C证型` | 只能说明关注某类辨证要素 |

### 4.2 路径比较算法

| 算法 | 通俗解释 | 为什么适合 |
|---|---|---|
| K短路径 | 找多条可能的辨证链 | 病案可能没写全，不能只看一条最短路径 |
| 路径类型序列比较 | 比较 `D-E-C`、`D-E`、`E-C` 这些结构 | 能看医家辨证思路结构是否相同 |
| 路径节点相似度 | 比较路径里的具体病因、病机、证型是否相同 | 能输出共同点和差异点 |
| 路径编辑距离 | 看两条路径要改几步才能变一样 | 可以量化医家辨证路径差异 |

线层最终输出：

| 输出 | 说明 |
|---|---|
| 共同辨证链 | 多位医家都有的 `病因-病机-证型` 组合 |
| 独有辨证链 | 某医家特有的辨证路径 |
| 路径完整度 | 每位医家的辨证链完整程度 |
| 路径差异分数 | 两位医家辨证路径差异大小 |

## 五、子图比较方案

面比较用于比较每位医家围绕同一病名形成的整体子图。

这里要明确：

> 子图只是医家辨证思维的结构化表示；子图相似度不是最终目的，最终目的是据此计算医家相似度。

例如：

| 子图 | 内容 |
|---|---|
| 张景岳-中风子图 | 张景岳与中风相关的证型、病因、病机及关系 |
| 叶天士-中风子图 | 叶天士与中风相关的证型、病因、病机及关系 |

### 5.1 比较主方案

| 算法 | 通俗解释 | 为什么适合 |
|---|---|---|
| 子图 Jaccard | 比较两个子图有多少共同节点和共同边 | 最直观，最适合作为主指标 |
| 关系类型分布比较 | 比较哪类关系占比高 | 能说明医家更偏向病因、病机还是证型 |

### 5.2 当前先尝试的向量方案

| 算法 | 通俗解释 | 为什么保留 |
|---|---|---|
| NetLSD | 用图整体频谱特征表示子图 | 对小图更稳，适合做整体结构对照 |
| FGSD | 用节点两两最短路谱分布表示子图 | 更强调局部到整体的结构差异，适合和 NetLSD 互相校对 |
| Graph2Vec | 用 Weisfeiler-Lehman 子结构把整个子图压成向量 | 适合补一个“子结构词袋”视角，和前两个纯谱方法互相对照 |

当前版本先落地 `NetLSD + FGSD + Graph2Vec`，三者一起做增强实验。

注意：

| 项目 | 说明 |
|---|---|
| 算法定位 | 只作增强实验，不替代主方案 |
| 语义边界 | `NetLSD` 和 `FGSD` 主要看结构，不直接理解中医语义标签 |
| 解读原则 | 如果向量分数和 Jaccard 不一致，先以可审计的子图 Jaccard 结论为主 |

### 5.3 子图导出边界

面层比较和向量实验都不导出“某病名下医家的所有松散连通点”，而是导出**医家-病名核心子图**。

定义如下：

| 项目 | 规则 |
|---|---|
| 锚点1 | 当前医家节点 `A医家` |
| 锚点2 | 当前病名节点 `B病名` |
| 可纳入节点 | 只纳入同时满足 `A医家 -> 该节点` 和 `B病名 -> 该节点` 的 `C证型 / D病因 / E病机` 节点 |
| 可纳入结构边 | 只保留已经纳入节点之间真实存在的 `C-D`、`C-E`、`D-E` 关系 |
| 必保留边 | `A医家-B病名`、`A医家-C/D/E`、`B病名-C/D/E` |
| 禁止纳入 | 只和医家有关、但和当前病名无关的 `C/D/E` 节点；只和病名有关、但当前医家没有提到的节点；其他病名上下文里的漂移节点 |

因此，一个医家在某病名下导出的子图，不是“这个医家的全部辨证图”，而是：

> 该医家对该病名已经明确落在当前图谱规则中的辨证认识核心子图

这样做的好处是边界硬、人工容易核对、后面跑 Graph2Vec 和 NetLSD 时不会混入别的病名语境。

### 5.4 为了尽量保留关系结构，向量导出采用关系节点化

Graph2Vec 和 NetLSD 对普通 NetworkX 图更友好，但原始关系类型如果直接丢掉，会损失语义。因此向量实验不直接把原始属性图喂进去，而是先做一次**关系节点化**：

| 原始关系 | 导出后的 NetworkX 结构 |
|---|---|
| `A医家 -[A医家-C证型]- C证型` | `A医家 -- R_A医家-C证型 -- C证型` |
| `B病名 -[B病名-E病机]- E病机` | `B病名 -- R_B病名-E病机 -- E病机` |
| `D病因 -[D病因-E病机]- E病机` | `D病因 -- R_D病因-E病机 -- E病机` |

规则说明：

| 项目 | 规则 |
|---|---|
| 原始实体节点标签 | 保留 `A/B/C/D/E` 类型 |
| 关系节点标签 | 新增 `R_关系类型` 标签 |
| 原始边方向 | 不讨论方向，统一转成无向结构用于子图相似度 |
| 原始数据库 | 不改库内存储，只在导出到 NetworkX 时转换 |

这样导出的向量图仍然可以反查回原始关系类型，也能避免把所有原始关系都压成一类普通边。

但要说清楚：

| 项目 | 说明 |
|---|---|
| 可保留的东西 | 关系在结构中的位置、锚点连接方式、局部拓扑差异 |
| 保留不了的东西 | 算法不会直接读懂“这个关系节点叫 `R_A医家-C证型`”这层语义 |
| 结论 | 这两个算法更适合做“结构像不像”的补充证据，不适合单独下中医语义结论 |

### 5.5 子图正确性校验

为了让你能确认“导出的这个子图到底对不对”，每次导出都必须同步生成 3 份可核对结果：

| 导出物 | 内容 | 用途 |
|---|---|---|
| 节点表 | 节点名、节点类型、纳入原因 | 看有没有多带或漏带节点 |
| 边表 | 起点、关系类型、终点 | 看边是否真实存在 |
| 审计表 | 每个 `C/D/E` 节点为什么被纳入，例如“同时满足 `A医家-C证型` 与 `B病名-C证型`” | 专门给人工核对子图正确性 |

人工核对规则固定如下：

| 检查项 | 正确标准 |
|---|---|
| 锚点数量 | 恰好 1 个 `A医家` 和 1 个 `B病名` |
| 节点类型 | 只允许 `A/B/C/D/E`，向量导出时额外允许 `R_关系类型` |
| `C/D/E` 节点纳入条件 | 必须同时有 `医家边` 和 `病名边` 作为证据 |
| 结构边合法性 | 只允许当前规则定义过的 10 类关系 |
| 孤立节点 | 不允许出现与锚点无证据关联的漂移节点 |
| 反查能力 | 任一节点和边都能回查到原始图谱里的真实记录 |

### 5.6 输出

| 输出 | 说明 |
|---|---|
| 医家相似矩阵（基于子图Jaccard） | 先比较两位医家的子图，再得到医家两两相似度 |
| 共同子结构 | 多位医家共享的节点和边 |
| 独有子结构 | 某位医家特有的图结构 |
| 结构差异解释 | 哪些节点或关系造成差异 |
| 医家相似矩阵（基于子图向量） | NetLSD、FGSD、Graph2Vec 三种向量法先比较子图，再得到医家两两相似度 |
| 子图审计清单 | 每位医家-病名子图的节点表、边表、审计表 |

## 六、论文指标设计

| 指标 | 计算方式 | 论文意义 |
|---|---|---|
| 共同节点数 | 多位医家共有的 `C/D/E` 节点数量 | 说明共同辨证认识 |
| 独有节点数 | 只属于某位医家的 `C/D/E` 节点数量 | 说明医家特色 |
| Node Similarity-Jaccard | 共同邻居 / 全部邻居 | 说明辨证要素重合程度 |
| Node Similarity-Overlap | 共同邻居 / 较小邻居集合 | 减少医案数量差异影响 |
| RWR得分 | 从医家和病名出发的节点重要性 | 找核心辨证要素 |
| 路径完整度 | 完整链数量 / 全部链数量 | 衡量辨证链条完整性 |
| 路径差异分数 | 路径编辑距离或路径集合差异 | 衡量辨证思路差异 |
| 子图Jaccard | 共同节点边 / 全部节点边 | 衡量整体结构相似性 |
| NetLSD 余弦相似度 | 比较两个核心子图的频谱向量夹角 | 作为整体结构相似性的增强证据 |
| FGSD 余弦相似度 | 比较两个核心子图的最短路谱分布向量夹角 | 作为局部结构差异的增强证据 |
| Graph2Vec 余弦相似度 | 比较两个核心子图的子结构向量夹角 | 作为子结构模式相似性的增强证据 |

## 七、系统实现建议

| 模块 | 实现内容 |
|---|---|
| 后端规则 | 将系统规则同步为当前 5 类实体、10 类关系 |
| 子图查询 | 按病名查询每位医家的核心子图，而不是松散连通子图 |
| 点比较服务 | 输出 Node Similarity 三种度量、RWR核心节点 |
| 线比较服务 | 输出 `A-D-E-C` 规范化辨证链、路径完整度、路径差异 |
| 面比较服务 | 输出子图Jaccard、关系类型分布、NetLSD、FGSD、Graph2Vec |
| 向量导出服务 | 将核心子图关系节点化后导出为 NetworkX 图 |
| 审计导出服务 | 输出节点表、边表、审计表，专门用于人工核对子图正确性 |
| 前端页面 | 医家比较页分为 `点比较`、`线比较`、`面比较`、`算法对比` 四个标签 |
| 导出功能 | 支持导出相似度矩阵、路径清单、子图指标、子图审计表为 CSV 或 Markdown |

## 八、推荐论文表述

可以写成：

> 本研究基于中医医案知识图谱，从点、线、面三个层面对不同医家治疗同一病名的辨证思维进行比较。点层采用 Neo4j GDS Node Similarity 算法，并选取 Jaccard、Overlap、Cosine 三种度量方式，比较医家在证型、病因、病机邻接集合上的相似程度；同时采用带重启随机游走识别各医家的核心辨证节点。线层依据中医临床辨证逻辑，将路径规范化为“医家 -> 病因 -> 病机 -> 证型”，比较不同医家的辨证链条差异。面层从医家疾病核心子图整体出发，采用子图 Jaccard 和关系类型分布衡量不同医家辨证结构的整体相似性，并以 NetLSD、FGSD、Graph2Vec 三种子图结构向量方法作为增强实验进行对照。

## 九、默认决策

| 问题 | 决策 |
|---|---|
| 是否比较治法、方剂、药物 | 不比较，当前规则不支持 |
| 研究主题 | 辨证思维比较，不写成完整治疗方案比较 |
| 点层主算法 | Node Similarity-Jaccard |
| 点层补充算法 | Node Similarity-Overlap、Node Similarity-Cosine、RWR |
| 线层路径顺序 | `A医家 -> D病因 -> E病机 -> C证型` |
| 是否使用 `证型 -> 病机` | 不作为临床辨证路径 |
| 面层主算法 | 子图 Jaccard + 关系类型分布 |
| 当前先试向量算法 | NetLSD + FGSD + Graph2Vec |
| 图嵌入算法定位 | 作为增强实验，不作为主结论 |
| 向量化对象 | 每个医家-病名核心子图，不是整张全局知识图谱 |
| 子图导出规则 | 只导出同时被当前医家和当前病名共同锚定的核心子图 |
| 是否保留关系类型 | 保留；导出到 NetworkX 时使用关系节点化 |
| 是否补全缺失路径 | 不补，缺失就记录为路径不完整 |
| 是否反转数据库关系 | 不反转，存储仍服从定义文件 |

## 十、用到的 skill

`tcm-kg-extractor`
